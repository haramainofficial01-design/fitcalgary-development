# Deprecated Node API prototype

This directory is retained only as a migration reference for the original API
contract and the shared PostgreSQL migration. It is not the production V1
backend and must not be deployed or extended.

The production backend is the Go service in `services/api-go`.
